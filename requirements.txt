torch>=1.13.0
torchvision>=0.14.0
transformers>=4.26.0
Pillow>=9.0.0
numpy>=1.21.0
matplotlib>=3.5.0
seaborn>=0.12.0
scikit-learn>=1.0.0
pandas>=1.3.0
tqdm>=4.64.0
Flask>=3.0.0
flask-cors>=4.0.0

# Hỗ trợ tiếng Việt và đa ngôn ngữ (M-CLIP)
# Cài thêm nếu muốn dùng MODEL_NAME = "M-CLIP/XLM-Roberta-Large-Vit-L-14"
multilingual-clip>=1.0.0
ftfy>=6.0.0

# Mixed precision training (đã có sẵn trong torch>=1.6, liệt kê để rõ ràng)
# torch.cuda.amp tích hợp sẵn, không cần cài thêm

# Werkzeug dùng bởi Flask (secure_filename)
Werkzeug>=2.0.0
